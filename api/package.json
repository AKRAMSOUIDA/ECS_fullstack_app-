{
  "name": "nodejs-api",
  "version": "1.0.0",
  "description": "Node.js API for fullstack app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"API tests would go here\" && exit 0",
    "lint": "echo \"API linting would go here\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
